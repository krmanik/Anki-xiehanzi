# Dependencies
/node_modules

# Production
/build

# Generated files
.docusaurus
.cache-loader
# Scraped pages, glyph SVGs and TTS clips the offline builders re-use
/.cache
.svelte-kit
/.vercel
/.netlify

# Misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

.backup
.config

Anki-xiehanzi - New HSK (2025).apkg
Anki-xiehanzi - New HSK (2025) with sentences.apkg

# Prebuilt per-level decks (uploaded as GitHub Release assets) + their audio cache
/dist-decks
/.cache

cedict.db
hsk_sentences.db

# Premium (private, not for publication)
/premium

# Local Claude Code settings (machine-specific paths).
.claude/
