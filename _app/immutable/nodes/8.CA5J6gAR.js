import{Tt as e,U as t,V as n,a as r,c as i,ct as a,lt as o,rt as s,ut as c,v as l,wt as u}from"../chunks/BJEJPKNH.js";import{s as d}from"../chunks/CidxQHrM.js";import"../chunks/xihTtKlq.js";import"../chunks/OUeeKfCn.js";import{t as f}from"../chunks/2hARvEBv.js";var p={title:`Export a Deck`},{title:m}=p,h=t(`<h1>Export an Anki deck</h1> <p>The <a>Create</a> tool builds a <code>.apkg</code> file entirely in your
browser — nothing is uploaded to a server. You design the card layout, add Chinese
words, then generate a deck you import straight into Anki.</p> <p>The page is a two-step wizard:</p> <ol><li><strong>Create Card Types</strong> — design what the cards look like.</li> <li><strong>Input Chinese Characters</strong> — add words and generate the <code>.apkg</code>.</li></ol> <p>You can move back and forth between the two steps at any time with the arrows at the
bottom of the page.</p> <h2>Step 1 — Design the cards</h2> <h3>Pick a card preset</h3> <p>Start from a preset (e.g. <em>meaning</em>, <em>pinyin / zhuyin</em>, <em>audio</em>, <em>tone marks</em>, <em>stroke writing</em>) instead of building from scratch. A preset fills in sensible front
and back fields. Presets that need pronunciation turn on <strong>Include Audio</strong> automatically.</p> <h3>Card types</h3> <p>A deck can hold several <strong>card types</strong> (the tabs labelled <em>Card 1</em>, <em>Card 2</em>, …).
Each card type becomes one card per word, so one word with three card types produces
three cards. For every card type you choose which fields appear on the <strong>front</strong> and
which on the <strong>back</strong>.</p> <h3>Available fields</h3> <ul><li><strong>Simplified</strong> / <strong>Traditional</strong> characters</li> <li><strong>Pinyin</strong> (tone-marked) and <strong>Zhuyin</strong> (Bopomofo)</li> <li><strong>Part of speech</strong></li> <li><strong>Meaning</strong> (short gloss) and full <strong>Definitions</strong> (CC-CEDICT)</li> <li><strong>Breakdown</strong>, <strong>Radical</strong>, <strong>HSK level</strong>, <strong>Frequency</strong></li> <li><strong>Examples</strong> — smart example sentences (pulls a separate ~5 MB sentence database
on demand, only when the field is used)</li> <li><strong>Audio</strong> — pre-recorded HSK audio, or text-to-speech for everything else</li> <li><strong>Writing component</strong> — the hanzi-writer stroke-order practice grid</li></ul> <p>Drag fields to reorder them; the order is shared across the whole deck. The
control-button bar and the separator line are layout-only tokens you can position
like any other field.</p> <h3>Appearance</h3> <p>The customizer controls fonts, mono vs. colored hanzi, tone-colored pinyin, the card
theme, whether definitions are collapsed, and example-sentence options (count and
min/max length). The live <strong>Preview</strong> on the right shows the front and back exactly
as Anki will render them.</p> <h2>Step 2 — Add words</h2> <p>Switch to <strong>Input Chinese Characters</strong>. Pick a source:</p> <table><thead><tr><th>Source</th><th>What it does</th></tr></thead><tbody><tr><td><strong>Word</strong></td><td>Type a single word (e.g. <code>中国</code>) and click <strong>Add</strong>.</td></tr><tr><td><strong>Paragraph</strong></td><td>Paste Chinese text; it is segmented into words automatically.</td></tr><tr><td><strong>File</strong></td><td>Upload a <code>.txt</code> file with one word per line.</td></tr><tr><td><strong>HSK Level</strong></td><td>Pick HSK levels 1–6 / 7+; every word at those levels is added, most-common first.</td></tr><tr><td><strong>BCT Level</strong></td><td>BCT A (~600 words) or B (~4000 words).</td></tr><tr><td><strong>YCT Level</strong></td><td>YCT levels 1–4.</td></tr></tbody></table> <p>Each added word is looked up for pinyin, meaning and the rest. Words appear in a
paginated table below where you can play audio, select rows, and <strong>Delete</strong> the ones
you don’t want.</p> <h2>Step 3 — Generate</h2> <p>Set the <strong>deck name</strong> at the top (it defaults to <code>Anki xiehanzi &lt;date&gt; &lt;timestamp&gt;</code>).</p> <ul><li><strong>Include Audio (Text-to-Speech)</strong> — when on, the export fetches pre-recorded HSK
audio where available and synthesizes the rest with edge-tts. This makes export
slower and the file larger. Two different note types are emitted depending on this
toggle (<code>Basic - (Anki-xiehanzi)</code> with audio, or the <code>- No Audio</code> variant).</li> <li><strong>Export CSV</strong> — download the raw word list as <code>.csv</code> (no cards/media).</li> <li><strong>Preview</strong> — final check of the generated cards before exporting.</li> <li><strong>Generate</strong> — builds the <code>.apkg</code>. A progress bar tracks media fetching (sidebar
icons, the writer engine + stroke data when a card uses the writing component, and
audio if enabled). When it finishes, the browser downloads <code>&lt;deck name&gt;.apkg</code>.</li></ul> <blockquote><p>Generating with audio for hundreds of words can take a while — TTS is fetched in
small batches with short delays. Keep the tab open until the download starts.</p></blockquote> <h2>Step 4 — Import into Anki</h2> <ol><li>Open <strong>Anki Desktop</strong>.</li> <li><strong>File → Import</strong> and choose the downloaded <code>.apkg</code>, or just double-click the file.</li> <li>The deck, note types, and all media (audio, fonts, stroke data) import together.</li></ol> <p>The same <code>.apkg</code> syncs to <strong>AnkiDroid</strong> and <strong>AnkiMobile</strong> through AnkiWeb.</p> <blockquote><p><strong>Back up first.</strong> Before importing into a collection that already has
scheduling/review history, make a backup with scheduling information. The cards use
JavaScript (Anki renders cards in a webview), so a few older or non-standard devices
may not display them correctly.</p></blockquote>`,1);function g(t,m){let g=r(m,[`children`,`$$slots`,`$$events`,`$$legacy`]);f(t,i(()=>g,()=>p,{children:(t,r)=>{var i=h(),f=c(o(i),2),p=c(a(f));u(3),e(f),u(50),s(()=>l(p,`href`,`${d??``}/create`)),n(t,i)},$$slots:{default:!0}}))}export{g as component};